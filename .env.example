# Public RPC URL (exposed to browser — no API keys here)
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# Server-only RPC URL (used by API routes — safe for API keys)
# Put your Helius/Quicknode/etc. URL with API key here
SOLANA_RPC_URL=

# Program IDs (deployed on devnet)
NEXT_PUBLIC_AGENT_REGISTRY_PROGRAM_ID=DiLuZ4JcnyFcE6FttH5NryQJrM2KKewy2Z8oDk9iJXNF
NEXT_PUBLIC_BOUNTY_ESCROW_PROGRAM_ID=2KY4RJwdYKnnDMU4WcuwgU2f8B7JoxjdKaTYL953AKb5

# USDC Mint (devnet test token)
NEXT_PUBLIC_USDC_MINT=6S5d1sgeLxQA2NiwuZ5CDryvxmLgWqs44da4XG3Nd4wZ

# Network
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Faucet mint authority — base58-encoded secret key of the devnet USDC mint authority
# Server-side only (no NEXT_PUBLIC_ prefix). Required for /api/faucet/usdc endpoint.
FAUCET_MINT_AUTHORITY=

# Upstash Redis — required for rate limiting on Vercel (serverless).
# Create a free database at https://console.upstash.com and paste the REST credentials.
# Optional for local dev (falls back to in-memory rate limiting).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
